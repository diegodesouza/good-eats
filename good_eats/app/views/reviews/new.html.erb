<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.select :rating, %w(1 2 3 4 5) %><hr><br>
  <%= f.label :body, "Add an Review:" %><br>
  <%= f.text_area :body %>
  <%= f.submit :class => "button" %>
<% end %>
